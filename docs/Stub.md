# Quick stub to quickly fill in tests and solution:
```
from LeetPride import run_process
from typing import List


class Solution:
    def method(self, parms) -> res:


def generate_tests():
    return [('Solution', None, True),
            ('method', [[1, 2], 'A'], [0, -1]),
            ]


def main() -> int:
    return run_process(generate_tests())


if __name__ == '__main__':
    exit(main())
```